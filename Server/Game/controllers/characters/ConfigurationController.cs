using Server.Game.objects.entities;

namespace Server.Game.controllers.characters
{
    class ConfigurationController : AbstractedSubController
    {
        public ConfigurationController(Character character) : base(character)
        {
        }
    }
}
using Server.Game.objects.implementable;

namespace Server.Game.objects.maps.objects.boxes
{
    class BonusBox : Box
    {
        public BonusBox(int id, string hash, Vector position, Spacemap map) : base(id, hash, position, map)
        {
        }
    }
}

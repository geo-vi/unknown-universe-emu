using Server.Game.objects.implementable;

namespace Server.Game.objects.maps.objects.boxes
{
    class PirateBootyBox : Box
    {
        public PirateBootyBox(int id, string hash, Vector position, Spacemap map) : base(id, hash, position, map)
        {
        }
    }
}
